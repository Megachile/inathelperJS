node_modules/
coverage/

# Tests don't need to be in the extension package
tests/
jest.config.js
package.json
package-lock.json

# Build artifacts (store zips are attached to GH releases, not committed)
*.zip
